name: Test Pipeline

on:
  push:

jobs:
  print-hello-world:
    runs-on: self-hosted
    steps:
      - name: Print Hello World
        run: echo "Hello, World!"

  list-local-files:
    runs-on: self-hosted
    steps:
      - name: List Local Files
        run: ls -la
